# DocAgent 端到端测试文档

## 审查结论

经过对项目代码、文档、提交历史的全面审查，**全部开发任务已完成**，构建验证结果如下：

| 验证项 | 结果 |
|--------|------|
| 前端 TypeScript 类型检查 + Vite 构建 | 通过 (1232 modules, 0 errors) |
| Rust 后端 cargo check | 通过 (0 errors, 0 warnings) |
| Rust 后端 cargo clippy | 通过 (0 warnings) |
| Python Sidecar 依赖 | 通过 (docx/openpyxl/pptx/fitz/reportlab 均可导入) |
| 应用启动运行 | 通过 (正常启动，无崩溃) |
| Git 提交历史 | 38 次提交，覆盖从项目初始化到功能完善全流程 |

### 已修复的问题

| 问题 | 修复内容 |
|------|----------|
| 前端错误处理器缺失更新模块错误码 | 补充 8001-8005 错误码映射、添加 update 模块类型、将 8001/8002/8005 加入可恢复错误码集合 |

---

## 前置条件

1. **运行环境**: Windows 11, Node.js, Python 3.12+, Rust 1.80+
2. **Python 依赖**: 已安装 `pip install -r sidecar/requirements.txt`
3. **LLM API Key**: 至少配置一个可用的 OpenAI 兼容 API（用于 Agent 交互测试）
4. **启动命令**: `npm run tauri:dev`

> **数据目录说明**: 应用数据存储在 `%APPDATA%\docagent` 目录下（即 `C:\Users\<用户名>\AppData\Roaming\docagent`），包含数据库文件 `docagent.db` 和配置目录 `config/`。

---

## 测试用例

### E2E-01: 应用启动与初始化

**目标**: 验证应用能正常启动，所有初始化流程正确执行

**步骤**:
1. 执行 `npm run tauri:dev` 启动应用
2. 观察应用窗口是否正常显示
3. 检查窗口尺寸是否为 1280x800（默认）
4. 检查窗口是否可调整大小
5. 检查自定义窗口控件（最小化/最大化/关闭）是否正常

**预期结果**:
- 应用窗口正常显示，无白屏或崩溃
- 顶部栏显示应用名称、工作区选择器和操作按钮
- 主区域显示"开始新会话"引导提示
- 右侧栏显示文件树、Agent 信息、Todo、Token 统计四个分区
- 底部输入框可正常聚焦
- 自定义窗口控件（最小化/最大化/关闭）正常工作

**验证点**:
- [ ] SQLite 数据库文件 `<app_data_dir>/docagent.db` 已创建
- [ ] 配置目录 `<app_data_dir>/config/` 已创建
- [ ] 日志文件 `log/docagent.log` 存在
- [ ] 无边框窗口正常显示，自定义标题栏可拖拽移动窗口

---

### E2E-02: LLM Provider 配置

**目标**: 验证 LLM Provider 的增删改查和连接测试（含多类型 Provider）

**步骤**:
1. 点击右上角设置图标，打开设置弹窗
2. 切换到 LLM 配置标签页
3. 点击"添加 Provider"按钮
4. 填写表单:
   - 名称: `Test OpenAI`
   - 类型: `OpenAI`
   - API 地址: `https://api.openai.com/v1`
   - API Key: 有效的 OpenAI API Key
   - 模型: `gpt-4o-mini`
5. 点击"测试连接"按钮
6. 保存配置
7. 验证 Provider 列表中出现新添加的条目
8. 将该 Provider 设为默认
9. 尝试编辑 Provider 名称
10. 尝试删除 Provider
11. 再次添加 Provider，类型选择 `Anthropic`，填写 Anthropic API Key 和模型 `claude-sonnet-4-20250514`
12. 测试 Anthropic 连接
13. 添加 Provider，类型选择 `Gemini`，填写 Gemini API Key 和模型 `gemini-2.0-flash`
14. 测试 Gemini 连接

**预期结果**:
- 连接测试返回成功，显示延迟和模型信息
- Provider 列表正确显示所有 Provider
- 设为默认操作成功，默认标记正确切换
- 编辑和删除操作正常
- Anthropic 类型 Provider 连接测试正常
- Gemini 类型 Provider 连接测试正常

**验证点**:
- [ ] 配置文件 `<app_data_dir>/config/llm_config.json` 正确保存
- [ ] 添加第一个 Provider 时自动设为默认
- [ ] 删除默认 Provider 后自动切换到第一个可用 Provider
- [ ] 无效 API Key 时连接测试返回明确的错误信息
- [ ] Anthropic 适配器使用 x-api-key 认证头
- [ ] Gemini 适配器使用 URL 参数认证
- [ ] Ollama 类型 Provider 可连接本地 Ollama 服务

---

### E2E-03: 工作区管理

**目标**: 验证工作区的添加、切换、移除功能

**步骤**:
1. 打开设置弹窗，切换到工作区标签页
2. 点击"添加工作区"
3. 选择一个本地目录路径，命名为 `测试工作区`
4. 保存后验证工作区列表中出现新条目
5. 点击"设为活动"切换到新工作区
6. 验证右侧栏文件树更新为新工作区的内容
7. 移除该工作区

**预期结果**:
- 工作区添加后列表即时更新
- 切换活动工作区后文件树刷新
- 移除工作区后列表更新，活动工作区自动回退

**验证点**:
- [ ] 配置文件 `<app_data_dir>/config/workspaces.json` 正确保存
- [ ] 文件树正确展示工作区目录结构
- [ ] 切换工作区后 Agent 的工作区路径同步更新
- [ ] 文件监听服务自动切换到新工作区

---

### E2E-04: Agent 对话交互（核心流程）

**目标**: 验证 Agent 完整的 Tool Calling 循环

**步骤**:
1. 确保已配置 LLM Provider 并设为默认
2. 确保已添加并激活一个工作区
3. 在输入框中输入: `帮我生成一份测试文档，标题为"项目周报"，内容包含本周工作总结和下周计划`
4. 按 Enter 发送
5. 观察工作流时间线:
   - 出现 User 节点（用户消息）
   - 出现 Thinking 节点（Agent 思考）
   - 出现 Tool 节点（调用 generate_document）
   - 出现 Result 节点（执行结果）
   - 出现 Reply 节点（Agent 最终回复）
6. 验证右侧栏 Todo 区域显示任务进度
7. 验证右侧栏 Token 统计更新

**预期结果**:
- 工作流时间线按顺序显示所有节点
- Agent 自动选择 generate_document 技能
- 文档在工作区目录中生成
- Agent 回复确认文档已生成
- Token 统计数字递增

**验证点**:
- [ ] 消息持久化到 SQLite（重启应用后可恢复）
- [ ] 流式输出过程中内容逐步显示
- [ ] Agent 执行期间输入框禁用
- [ ] Agent 执行完成后输入框恢复可用
- [ ] 节点可展开/折叠查看详情

---

### E2E-05: 文档生成（多格式）

**目标**: 验证多种格式文档的生成功能

**步骤**:
1. 发送消息: `生成一份 Word 文档，保存为 test_report.docx，标题是"测试报告"，内容包含三个段落`
2. 等待 Agent 执行完成
3. 在工作区目录中找到 `test_report.docx`
4. 发送消息: `生成一份 Excel 文档，保存为 test_data.xlsx，包含一个学生成绩表`
5. 等待 Agent 执行完成
6. 发送消息: `生成一份 PPT 文档，保存为 test_slides.pptx，主题是"产品介绍"，包含3张幻灯片`
7. 等待 Agent 执行完成
8. 发送消息: `生成一份 Markdown 文档，保存为 test_notes.md，内容是会议纪要`
9. 等待 Agent 执行完成
10. 发送消息: `生成一份 PDF 文档，保存为 test_summary.pdf，内容是项目摘要`
11. 等待 Agent 执行完成

**预期结果**:
- 所有格式的文档成功生成
- Word 文档包含标题和段落
- Excel 文档包含表格数据
- PPT 文档包含幻灯片
- Markdown 文件内容正确
- PDF 文件可正常打开

**验证点**:
- [ ] Sidecar 进程正常启动和通信
- [ ] 生成的 .docx 文件可正常打开
- [ ] 生成的 .xlsx 文件可正常打开
- [ ] 生成的 .pptx 文件可正常打开
- [ ] 生成的 .md 文件内容正确
- [ ] 生成的 .pdf 文件可正常打开
- [ ] 文档属性中作者字段正确

---

### E2E-06: 文档读取

**目标**: 验证读取已有文档内容

**步骤**:
1. 发送消息: `读取 test_report.docx 的内容`
2. 等待 Agent 执行完成
3. 观察 Agent 回复中是否包含文档内容
4. 发送消息: `读取 test_data.xlsx 的内容`
5. 等待 Agent 执行完成
6. 发送消息: `读取 test_slides.pptx 的内容`
7. 等待 Agent 执行完成

**预期结果**:
- Agent 调用 read_document 技能
- 返回文档的段落、表格、属性信息
- Agent 将内容整理后回复给用户

**验证点**:
- [ ] 不存在的文件路径返回明确的错误信息
- [ ] 读取结果包含段落数和表格数统计
- [ ] Excel 读取返回工作表名称和单元格数据
- [ ] PPT 读取返回幻灯片内容和形状信息

---

### E2E-07: 文档修改

**目标**: 验证修改已有文档

**步骤**:
1. 发送消息: `修改 test_report.docx，将第一段文字替换为"这是修改后的内容"`
2. 等待 Agent 执行完成
3. 重新读取文档验证修改结果

**预期结果**:
- Agent 调用 modify_document 技能
- 由于 modify_document 是高风险操作，弹出确认对话框
- 用户确认后执行修改
- 修改结果正确反映在文档中

**验证点**:
- [ ] 高风险操作触发确认机制
- [ ] 用户拒绝时操作被取消
- [ ] 修改后文档内容正确更新
- [ ] 修改前自动创建版本快照

---

### E2E-08: 操作确认机制

**目标**: 验证高风险操作的用户确认流程

**步骤**:
1. 发送消息: `删除 test_report.docx`
2. 观察工作流时间线出现确认节点
3. 点击"确认执行"按钮
4. 验证文件被删除
5. 重新生成文档后，再次尝试删除，这次点击"取消操作"
6. 验证文件未被删除

**预期结果**:
- 删除操作触发确认对话框（风险等级: critical）
- 确认后文件被删除（可选创建备份）
- 取消后操作被跳过，Agent 回复"用户拒绝了操作"

**验证点**:
- [ ] 确认节点显示操作类型、描述、风险等级
- [ ] 确认超时（300秒）后自动取消
- [ ] 路径安全校验：不允许删除工作区外的文件

---

### E2E-09: 会话管理

**目标**: 验证会话的创建、切换、删除、标题更新

**步骤**:
1. 点击顶部栏"新建会话"按钮（或 Ctrl+N）
2. 验证工作流时间线清空，进入新会话
3. 发送一条消息，观察会话标题自动更新
4. 点击顶部栏"历史"按钮，打开历史面板
5. 在历史面板中切换到之前的会话
6. 关闭历史面板，验证工作流显示切换后的会话内容
7. 在历史面板中删除一个会话

**预期结果**:
- 新建会话后时间线清空
- 历史面板正确列出所有会话
- 切换会话后内容正确加载
- 删除会话后列表更新

**验证点**:
- [ ] 会话数据持久化到 SQLite
- [ ] 删除当前会话后自动切换到下一个可用会话
- [ ] 会话标题更新后历史面板同步刷新
- [ ] 清除所有会话功能正常工作

---

### E2E-10: Agent 停止

**目标**: 验证 Agent 执行过程中可以手动停止

**步骤**:
1. 发送一个需要较长时间处理的请求
2. 在 Agent 执行期间点击"停止"按钮
3. 观察 Agent 是否中断执行

**预期结果**:
- Agent 收到停止信号后中断当前循环
- 工作流时间线显示已完成的步骤
- 已执行的消息已持久化到数据库
- 输入框恢复可用状态

**验证点**:
- [ ] 停止后 Agent 不再发送 LLM 请求
- [ ] 已完成的 Tool 结果被保留
- [ ] 停止后可以继续发送新消息
- [ ] 停止过程中显示 stopping 状态

---

### E2E-11: Skill 管理（含自定义 Skill）

**目标**: 验证 Skill 的启用/禁用功能和自定义 Skill 管理

**步骤**:
1. 打开设置弹窗，切换到 Skills 标签页
2. 查看已注册的 9 个内置 Skill 列表
3. 禁用 `generate_document` Skill
4. 保存设置
5. 尝试让 Agent 生成文档
6. 重新启用该 Skill
7. 点击"添加自定义 Skill"按钮
8. 填写自定义 Skill 表单:
   - 名称: `合同生成器`
   - 描述: `根据参数生成合同文档`
   - 分类: `文档处理`
   - 支持的文档类型: `docx`
   - 提示词模板: `你是一位专业的{{domain}}专家。请根据以下要求生成一份{{docType}}文档：主题：{{topic}}`
9. 保存自定义 Skill
10. 验证自定义 Skill 出现在列表中
11. 编辑自定义 Skill，修改描述
12. 删除自定义 Skill

**预期结果**:
- Skill 列表正确显示 9 个内置技能
- 禁用后 Agent 无法调用该技能
- 重新启用后恢复正常
- 自定义 Skill 创建成功并显示在列表中
- 编辑和删除自定义 Skill 正常

**验证点**:
- [ ] 禁用状态持久化到 `app_settings.json`
- [ ] 禁用的 Skill 不出现在 LLM 的 tool_definitions 中
- [ ] 应用重启后禁用状态保持
- [ ] 自定义 Skill 配置文件保存在 `<app_data_dir>/config/custom_skills/` 目录
- [ ] 自定义 Skill 的 `{{param}}` 占位符参数自动识别
- [ ] 删除自定义 Skill 后配置文件同步删除

---

### E2E-12: 应用设置（完整 9 个标签页）

**目标**: 验证应用设置的所有标签页功能

**步骤**:
1. 打开设置弹窗，验证 9 个标签页均存在: LLM 配置、工作区管理、Skills 管理、Prompt 模板、Token 用量、外观设置、快捷键、通用设置、帮助
2. 切换到通用设置标签页
3. 修改作者名称为 `测试用户`
4. 修改确认级别为 `Always`
5. 保存设置
6. 切换到外观设置标签页
7. 切换主题模式为"深色"
8. 调整字体缩放为 120%
9. 切换主题模式为"跟随系统"
10. 切换到快捷键标签页
11. 点击"新建会话"快捷键，按下 Ctrl+Shift+N
12. 点击"恢复默认"按钮
13. 重启应用，验证设置已持久化

**预期结果**:
- 9 个标签页全部正常显示
- 设置修改后即时生效
- 主题切换后界面颜色立即更新
- 字体缩放调整后界面字体大小变化
- 快捷键自定义功能正常
- 重启后设置值保持

**验证点**:
- [ ] `app_settings.json` 文件正确保存
- [ ] 新增字段有默认值（merge_with_defaults 逻辑）
- [ ] Token 预算和版本快照设置可配置
- [ ] 深色/浅色/跟随系统三种主题模式正常切换
- [ ] 字体缩放范围 80%-140% 正常工作
- [ ] 快捷键捕获功能正常
- [ ] 恢复默认快捷键功能正常

---

### E2E-13: 文档搜索

**目标**: 验证工作区内文档搜索功能

**步骤**:
1. 发送消息: `搜索工作区中所有 .docx 文件`
2. 等待 Agent 执行完成
3. 验证返回结果包含文件名、路径、大小等信息

**预期结果**:
- Agent 调用 search_documents 技能
- 返回匹配的文件列表
- 结果按相关性排序

**验证点**:
- [ ] 搜索范围限制在工作区内
- [ ] 支持按扩展名筛选
- [ ] 支持按文件名和内容搜索

---

### E2E-14: 文档分析

**目标**: 验证文档分析功能

**步骤**:
1. 先生成一份测试文档
2. 发送消息: `分析 test_report.docx 的结构`
3. 等待 Agent 执行完成

**预期结果**:
- 返回文档统计信息（段落数、字数、标题数等）
- 返回标题层级结构
- 返回文档属性信息

**验证点**:
- [ ] 分析结果包含完整的统计信息
- [ ] 标题层级正确提取

---

### E2E-15: 错误处理与自动恢复

**目标**: 验证各种错误场景的处理和自动恢复机制

**步骤**:
1. 未配置 LLM Provider 时发送消息 -> 应提示配置 Provider
2. 读取不存在的文件 -> 应返回文件未找到错误
3. 删除工作区外的文件 -> 应拒绝操作
4. LLM API Key 无效 -> 应返回认证失败错误
5. Sidecar 进程异常退出 -> 应自动重启并重试
6. Agent 执行出错时，观察工作流时间线是否出现错误节点
7. 可恢复的错误节点是否显示"重试"按钮
8. 点击"重试"按钮，验证是否重新发送消息

**预期结果**:
- 每种错误场景都有明确的错误提示
- 错误信息对用户友好
- 可恢复的错误不会导致应用崩溃
- 错误节点显示错误码、模块、可恢复标记
- 可恢复错误提供重试按钮

**验证点**:
- [ ] 错误事件正确发射到前端
- [ ] Agent 执行失败后状态正确恢复
- [ ] Sidecar 自动重启机制有效
- [ ] 错误节点显示错误码和模块信息
- [ ] 可恢复错误显示"重试"按钮
- [ ] 重试功能正常工作
- [ ] Toast 通知正确显示错误信息
- [ ] 可恢复错误使用 warning 类型 Toast，不可恢复使用 error 类型

---

### E2E-16: 文档预览

**目标**: 验证文档预览功能（含多格式渲染）

**步骤**:
1. 在右侧栏文件树中双击一个 .docx 文件
2. 验证预览浮层打开，显示 Word 文档结构化内容（段落、标题、表格）
3. 关闭预览
4. 双击一个 .xlsx 文件
5. 验证预览浮层显示 Excel 表格（含工作表标签、表头、数据行）
6. 关闭预览
7. 双击一个 .pptx 文件
8. 验证预览浮层显示 PPT 幻灯片内容（逐幻灯片渲染）
9. 关闭预览
10. 双击一个 .md 文件
11. 验证预览浮层使用 Markdown 渲染（支持 GFM 表格、代码高亮）
12. 关闭预览
13. 双击一个 .pdf 文件
14. 验证预览浮层使用 pdfjs-dist Canvas 渲染 PDF 页面
15. 测试 PDF 预览的缩放、翻页功能

**预期结果**:
- 各格式文档预览正常打开
- Word 文档显示段落、标题层级、表格
- Excel 文档显示工作表标签和表格数据
- PPT 文档逐幻灯片显示内容
- Markdown 文档正确渲染（GFM、代码高亮）
- PDF 文档使用 Canvas 渲染，支持缩放和翻页

**验证点**:
- [ ] 预览浮层 ESC 关闭功能正常
- [ ] 预览加载中显示加载状态
- [ ] PDF 预览失败时降级为文本模式
- [ ] 预览浮层不遮挡其他操作
- [ ] 预览浮层懒加载，不影响首屏性能

---

### E2E-17: 版本历史管理

**目标**: 验证文档版本历史的查看、对比和回滚功能

**步骤**:
1. 在文件树中右键点击一个已修改过的文档
2. 选择"版本历史"
3. 验证版本历史面板打开，显示版本列表
4. 验证每个版本显示操作类型（创建/修改/转换/回滚）、时间戳、描述
5. 勾选两个版本，点击"对比选中版本"
6. 验证差异对比视图打开，显示并排对比或内联对比
7. 切换对比视图模式（并排/内联）
8. 点击某个历史版本的"与当前版本对比"按钮
9. 验证差异对比视图显示历史版本与当前版本的差异
10. 点击某个历史版本的"回滚"按钮
11. 在确认对话框中确认回滚
12. 验证文件内容已回滚到历史版本

**预期结果**:
- 版本历史面板正确显示所有版本
- 版本对比功能正常，差异高亮显示
- 并排对比和内联对比两种模式可切换
- 回滚功能正常，回滚后文件内容更新
- 回滚操作需要确认

**验证点**:
- [ ] 版本列表按时间倒序排列
- [ ] 最新版本标记为"当前"
- [ ] 差异统计显示新增/删除行数
- [ ] 并排对比模式同步滚动
- [ ] 回滚后版本列表更新（新增 rollback 类型版本）
- [ ] 回滚确认对话框正常弹出
- [ ] ESC 关闭版本历史面板

---

### E2E-18: 格式转换

**目标**: 验证文档格式转换功能

**步骤**:
1. 发送消息: `将 test_report.docx 转换为 PDF 格式`
2. 等待 Agent 执行完成
3. 验证工作区中生成对应的 PDF 文件
4. 发送消息: `将 test_notes.md 转换为 PDF 格式`
5. 等待 Agent 执行完成
6. 验证工作区中生成对应的 PDF 文件

**预期结果**:
- 格式转换成功完成
- 转换后的文件格式正确
- Agent 回复确认转换结果

**验证点**:
- [ ] Word 转 PDF 正常
- [ ] Markdown 转 PDF 正常
- [ ] Excel 转 PDF 正常
- [ ] PPT 转 PDF 正常
- [ ] 不支持的转换组合返回明确错误

---

### E2E-19: 批量处理

**目标**: 验证批量处理功能

**步骤**:
1. 确保工作区中有多个文档
2. 发送消息: `将工作区中所有 .docx 文件转换为 PDF 格式`
3. 等待 Agent 执行完成
4. 验证所有 .docx 文件都已转换为 PDF

**预期结果**:
- Agent 调用 batch_process 技能
- 批量转换成功完成
- 每个文件的处理结果都有反馈

**验证点**:
- [ ] 批量操作逐个处理每个文件
- [ ] 部分文件失败不影响其他文件处理
- [ ] 批量操作结果汇总显示

---

### E2E-20: Prompt 模板管理

**目标**: 验证 Prompt 模板的创建、编辑、删除和使用

**步骤**:
1. 打开设置弹窗，切换到 Prompt 模板标签页
2. 点击"创建模板"按钮
3. 填写模板表单:
   - 名称: `周报生成`
   - 描述: `生成项目周报文档`
   - 内容: `请帮我生成一份项目周报文档，标题为"{{title}}"，包含以下部分：1. 本周工作总结 2. 遇到的问题 3. 下周计划`
4. 保存模板
5. 验证模板列表中出现新条目
6. 编辑模板，修改内容
7. 在输入框中点击模板选择器，选择刚创建的模板
8. 验证输入框填充模板内容
9. 删除模板

**预期结果**:
- 模板创建成功
- 模板列表正确显示
- 编辑和删除功能正常
- 模板选择器可正常使用

**验证点**:
- [ ] 模板数据持久化到 SQLite
- [ ] 模板支持变量占位符
- [ ] 模板选择器正确显示所有模板
- [ ] 删除模板后列表更新

---

### E2E-21: Token 用量统计

**目标**: 验证 Token 用量统计和预算管理功能

**步骤**:
1. 打开设置弹窗，切换到 Token 用量标签页
2. 查看用量趋势图表
3. 查看 Provider/Model 分组用量
4. 查看用量概览（今日/本月/总计）
5. 切换到通用设置标签页
6. 配置 Token 预算: 日限额 100000，月限额 3000000
7. 设置超出行为为"仅提醒"
8. 保存设置

**预期结果**:
- 用量趋势图表正确显示
- Provider 分组用量正确统计
- 概览数据准确
- 预算配置保存成功

**验证点**:
- [ ] 趋势图表使用 Recharts 渲染
- [ ] 右侧栏 Token 统计实时更新
- [ ] 预算超出时触发提醒
- [ ] Token 用量数据持久化到 SQLite

---

### E2E-22: 文件树操作

**目标**: 验证文件树的右键菜单操作

**步骤**:
1. 在右侧栏文件树中右键点击一个文件
2. 验证上下文菜单包含: 预览、版本历史、在文件管理器中显示、重命名、删除
3. 选择"预览"，验证预览浮层打开
4. 选择"在文件管理器中显示"，验证文件管理器打开
5. 选择"重命名"，输入新名称，确认
6. 验证文件已重命名
7. 右键点击空白区域
8. 验证上下文菜单包含: 新建文件、新建文件夹
9. 选择"新建文件"，输入文件名
10. 验证新文件已创建

**预期结果**:
- 右键菜单正确显示
- 预览功能正常
- 在文件管理器中显示功能正常
- 重命名功能正常
- 新建文件/文件夹功能正常

**验证点**:
- [ ] 文件树双击触发预览
- [ ] 文件变更后文件树自动刷新（文件监听）
- [ ] 删除文件需要确认
- [ ] 路径安全校验：不允许在工作区外操作

---

### E2E-23: 配置导入导出

**目标**: 验证应用配置的导入导出功能

**步骤**:
1. 打开设置弹窗，切换到通用设置标签页
2. 点击"导出配置"按钮
3. 选择保存路径，确认导出
4. 验证导出的 JSON 文件内容正确
5. 修改某些设置
6. 点击"导入配置"按钮
7. 选择之前导出的 JSON 文件
8. 确认导入
9. 验证设置已恢复为导出时的状态

**预期结果**:
- 导出的配置文件格式正确
- 导入后设置正确恢复
- 导入时可选择是否覆盖

**验证点**:
- [ ] 导出配置包含 LLM Provider、工作区、应用设置
- [ ] 导出时可选是否包含 API Key 等敏感信息
- [ ] 导入配置后所有设置正确恢复
- [ ] 导入失败时显示明确错误信息

---

### E2E-24: 应用自动更新

**目标**: 验证应用自动更新检查功能

**步骤**:
1. 启动应用，等待 5 秒后观察是否自动检查更新
2. 如果有更新，验证更新通知组件显示
3. 点击"稍后提醒"关闭通知
4. 打开设置，手动触发更新检查
5. 验证更新检查流程

**预期结果**:
- 应用启动后自动检查更新（可配置）
- 发现新版本时显示更新通知
- 更新通知显示版本号和更新说明
- 下载进度条正常显示
- 安装完成后提示重启

**验证点**:
- [ ] 自动检查更新可通过设置开关控制
- [ ] 更新检查失败时静默处理（不弹窗）
- [ ] 下载进度百分比和文件大小正确显示
- [ ] 更新安装完成后提供重启按钮
- [ ] 开发模式下更新服务器不可达时正常降级

---

### E2E-25: 帮助面板

**目标**: 验证帮助面板的内容和功能

**步骤**:
1. 打开设置弹窗，切换到帮助标签页
2. 验证快速入门步骤显示
3. 验证快捷键列表显示
4. 验证内置 Skill 列表显示（9 个）
5. 验证常见问题列表显示

**预期结果**:
- 帮助面板内容完整
- 快捷键列表与实际功能一致
- 内置 Skill 列表正确
- 常见问题内容有帮助

**验证点**:
- [ ] 快速入门包含 3 个步骤
- [ ] 快捷键列表包含 8 个快捷键
- [ ] 内置 Skill 列表包含 9 个技能
- [ ] 常见问题包含 8 个问题

---

### E2E-26: LLM 健康检查与 Provider 切换

**目标**: 验证 LLM Provider 健康检查和自动切换机制

**步骤**:
1. 配置至少两个 LLM Provider
2. 将其中一个设为默认
3. 在设置中点击"健康检查"按钮
4. 验证所有 Provider 的健康状态更新
5. 手动切换默认 Provider
6. 验证 Provider 切换事件正确发射

**预期结果**:
- 健康检查显示每个 Provider 的连接状态
- Provider 切换后后续请求使用新 Provider
- 不可用的 Provider 自动标记

**验证点**:
- [ ] 每 5 分钟自动执行健康检查
- [ ] 不可用 Provider 5 分钟后自动恢复尝试
- [ ] Provider 切换时发射 `llm:provider_switch` 事件
- [ ] Fallback 机制正常工作

---

### E2E-27: 错误边界与虚拟滚动

**目标**: 验证应用的健壮性和性能优化

**步骤**:
1. 在开发者工具中模拟组件渲染异常
2. 验证错误边界捕获异常，显示恢复页面
3. 点击"恢复页面"按钮
4. 创建大量工作流节点（超过 50 个）
5. 验证虚拟滚动正常工作，页面不卡顿

**预期结果**:
- 错误边界捕获渲染异常
- 提供恢复和重启操作
- 虚拟滚动保证大量节点时性能正常

**验证点**:
- [ ] ErrorBoundary 包裹应用根组件
- [ ] 渲染异常时显示错误信息和恢复按钮
- [ ] 工作流时间线使用虚拟滚动
- [ ] 文件树使用虚拟滚动
- [ ] 懒加载组件（PreviewOverlay、SettingsDialog、HistoryPanel）不影响首屏

---

## 测试结果汇总表

| 编号 | 测试用例 | 功能模块 | 结果 |
|------|----------|----------|------|
| E2E-01 | 应用启动与初始化 | 基础框架 | |
| E2E-02 | LLM Provider 配置（含多类型） | LLM | |
| E2E-03 | 工作区管理 | 工作区 | |
| E2E-04 | Agent 对话交互 | Agent | |
| E2E-05 | 文档生成（多格式） | Skill/Sidecar | |
| E2E-06 | 文档读取 | Skill/Sidecar | |
| E2E-07 | 文档修改 | Skill/Sidecar | |
| E2E-08 | 操作确认机制 | Agent | |
| E2E-09 | 会话管理 | 会话 | |
| E2E-10 | Agent 停止 | Agent | |
| E2E-11 | Skill 管理（含自定义 Skill） | Skill | |
| E2E-12 | 应用设置（完整 9 个标签页） | 设置 | |
| E2E-13 | 文档搜索 | Skill | |
| E2E-14 | 文档分析 | Skill | |
| E2E-15 | 错误处理与自动恢复 | 全局 | |
| E2E-16 | 文档预览 | 预览 | |
| E2E-17 | 版本历史管理 | 版本 | |
| E2E-18 | 格式转换 | Skill/Sidecar | |
| E2E-19 | 批量处理 | Skill | |
| E2E-20 | Prompt 模板管理 | 模板 | |
| E2E-21 | Token 用量统计 | Token | |
| E2E-22 | 文件树操作 | 文件系统 | |
| E2E-23 | 配置导入导出 | 配置 | |
| E2E-24 | 应用自动更新 | 更新 | |
| E2E-25 | 帮助面板 | 帮助 | |
| E2E-26 | LLM 健康检查与 Provider 切换 | LLM | |
| E2E-27 | 错误边界与虚拟滚动 | 性能/健壮性 | |

---

## 功能完成度评估

### 基础框架 (100%)
- Tauri 2 + React + TypeScript 项目结构完整
- 无边框窗口 + 自定义窗口控件
- SQLite 数据库初始化（6 张表 + 索引 + 版本记录）
- JSON 配置管理（app_settings / llm_config / workspaces / custom_skills）
- 双输出日志系统（控制台 + 文件）
- 数据库损坏检测和自动恢复

### LLM 引擎 (100%)
- LlmProvider trait 定义完整，支持 chat / chat_stream / test_connection
- OpenAI 适配器实现完整（含流式 SSE 解析、重试逻辑、错误处理）
- Anthropic 适配器实现完整（原生 Messages API、x-api-key 认证、content block 解析）
- Gemini 适配器实现完整（原生 Gemini API、URL 参数认证）
- LlmRouter 支持默认选择、Fallback 切换和健康检查
- 每 5 分钟自动健康检查，不可用 Provider 自动标记和恢复

### Agent 引擎 (100%)
- AgentExecutor 实现 Tool Calling 循环（最大 20 轮迭代）
- 增量持久化回调防止崩溃丢失消息
- Tauri 事件系统完整（9 个 Agent 事件 + 系统事件）
- useAgent Hook 封装完整（事件监听、状态管理、组件卸载清理）
- 操作确认机制（高风险 Skill 触发确认、oneshot channel 同步等待、5 分钟超时）
- Agent 停止功能（should_stop 闭包检查、stopping 状态转换）
- 错误节点与自动恢复（ErrorNode 组件、可恢复标记、重试按钮）

### Skill 系统 (100%)
- Skill trait 定义完整（skill_name / description / parameters / execute）
- SkillRegistry 支持注册、查询、启用/禁用、工具定义生成
- 9 个内置 Skill 全部实现（generate / read / modify / delete / convert / search / analyze / list_workspace / batch_process）
- 自定义 Skill 系统（CustomSkillLoader、JSON 配置文件、Prompt 模板、{{param}} 占位符）
- Python Sidecar 进程管理器（启动/停止/自动重启/超时/重试）
- Sidecar stdin/stdout JSON 行协议完整
- 5 种文档处理器完整（Word / Excel / PPT / PDF / Markdown，各支持 generate / read / modify / convert / analyze）

### 主界面 (100%)
- TopBar 组件（历史/新建/设置按钮、工作区选择器）
- MainLayout 布局（主区域 + 右侧栏）
- WorkflowTimeline + 8 种节点类型（User / Thinking / Tool / Result / Reply / Confirm / Error / WorkflowNode）
- InputArea 输入框（自动高度、快捷键、模板标签）
- 右侧栏四个分区（FileTree / AgentInfo / Todo / Token）
- SettingsDialog 设置弹窗（9 个标签页: LLM / 工作区 / Skills / 模板 / Token / 外观 / 快捷键 / 通用 / 帮助）

### 会话与确认 (100%)
- 会话 CRUD（create / list / get / delete / update_title / clear_all）
- 会话事件发射（session:updated）
- 版本快照仓库（create / list / delete / get_content / rollback）
- 历史会话面板（左侧滑出、会话切换、删除当前会话后自动切换）
- 操作确认机制（高风险 Skill 触发确认、oneshot channel 同步等待、超时处理）
- ConfirmNode 组件（确认/取消按钮、状态展示）

### 文档预览 (100%)
- PreviewOverlay 预览浮层（ESC 关闭、加载状态）
- MarkdownPreview 组件（react-markdown + remark-gfm + rehype-highlight）
- PdfCanvasViewer 组件（pdfjs-dist Canvas 渲染、缩放、翻页）
- WordDocumentView 结构化渲染（段落、标题层级、表格、属性）
- ExcelTableRenderer 表格渲染（工作表标签、表头、数据行）
- PptDocumentView 结构化渲染（幻灯片、形状、文本）
- DiffView 差异对比（并排对比、内联对比、同步滚动、差异统计）

### 版本历史 (100%)
- VersionHistoryPanel 版本历史面板（版本列表、操作类型标记、时间戳）
- 版本对比功能（选中两个版本对比、与当前版本对比）
- 版本回滚功能（确认对话框、回滚后刷新文件树和版本列表）

### 设置系统 (100%)
- LLMConfig: Provider 增删改查、连接测试、默认设置、健康检查
- WorkspaceTab: 工作区增删切换
- SkillsTab: 内置 Skill 启用/禁用、自定义 Skill 增删改
- TemplatesTab: Prompt 模板增删改查
- TokenUsageTab: 用量趋势、Provider 分组、概览、预算状态
- AppearanceTab: 主题模式（浅色/深色/跟随系统）、字体缩放（80%-140%）
- ShortcutsTab: 快捷键自定义、捕获、恢复默认
- GeneralTab: 作者名、确认级别、Token 预算、版本快照策略、配置导入导出
- HelpTab: 快速入门、快捷键列表、内置 Skill 列表、常见问题

### 更新系统 (100%)
- tauri-plugin-updater 集成
- UpdateNotification 组件（检查/下载/安装/重启流程）
- 自动检查更新（启动后 5 秒、可配置开关）
- 下载进度显示（百分比 + 文件大小）
- 更新错误处理和重试

### 性能优化 (100%)
- 懒加载（PreviewOverlay、SettingsDialog、HistoryPanel、VersionHistoryPanel、UpdateNotification）
- 虚拟滚动（工作流时间线、文件树）
- ErrorBoundary 错误边界（应用根组件包裹）
- Toast 通知（全局容器、3 秒自动消失、最大 5 条）

### 文件系统 (100%)
- 文件树展示和交互（双击预览、右键菜单）
- 文件操作（创建文件/目录、重命名、删除、在文件管理器中显示）
- 文件监听服务（notify crate、递归监听、事件发射、自动切换）
- 路径安全校验（拒绝路径遍历攻击）
